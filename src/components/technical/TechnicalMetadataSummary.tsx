import type { TechnicalMetadata } from "@/features/technical/types";

type TechnicalMetadataSummaryProps = {
  metadata?: TechnicalMetadata;
  compact?: boolean;
};

export function TechnicalMetadataSummary(_props: TechnicalMetadataSummaryProps) {
  return null;
}

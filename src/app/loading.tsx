import { LoadingState } from "@/components/ui/StateMessage";

export default function Loading() {
  return <LoadingState title="Carregando plataforma" description="Organizando cursos, módulos e aulas." />;
}
